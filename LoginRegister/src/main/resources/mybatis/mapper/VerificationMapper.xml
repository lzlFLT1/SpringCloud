<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kasei.springcloud.loginregister.repository.dao.VerificationMapper">
    
    <select id="getById" resultType="Verification">
        select * from KsfDemo1_verification
        where id=#{id}
    </select>

</mapper>